# VARNOX X CRASHER — Bot (VPS/Pterodactyl)
# Optional: when set, the premium whitelist also loads from the shared Neon DB
# (table: crasher_bot_premium). Without it the bot uses ./database/premium.json as before.
DATABASE_URL=postgresql://user:password@ep-xxxx.aws.neon.tech/neondb?sslmode=require
